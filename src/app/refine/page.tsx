import RefineTool from "@/components/RefineTool";

export default function RefinePage() {
  return (
    <div className="min-h-full bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-fuchsia-950/30 via-zinc-950 to-zinc-950">
      <main>
        <RefineTool />
      </main>
    </div>
  );
}
