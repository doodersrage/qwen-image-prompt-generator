import LintTool from "@/components/LintTool";

export default function LintPage() {
  return (
    <div className="min-h-full bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-amber-950/30 via-zinc-950 to-zinc-950">
      <main>
        <LintTool />
      </main>
    </div>
  );
}
